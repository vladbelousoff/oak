import shapes.geometry;

let p = geometry.make_point(3, 4);
print(p.x);
print(p.y);

let p2 = new geometry.Point { x: 10, y: 20 };
print(p2.x);
print(p2.y);

let p3 = geometry.scale(p, 2);
print(p3.x);
print(p3.y);

print(geometry.sum_coords(p));
