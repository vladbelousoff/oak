let mut arr = [] as number[];
arr.push(1);
arr.push(2);
arr.push(3);
arr.push(4);
arr.push(5);

print(arr.len());

let mut sum = 0;
for i from 0 to arr.len() {
  sum += arr[i];
}
print(sum);

arr[0] = 100;
arr[arr.len() - 1] = 500;
print(arr[0]);
print(arr[4]);
