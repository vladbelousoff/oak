let src =
    'fn add(a : number, b : number) -> number { return a + b; }\n' +
    'let x = add(1, 2);\n' +
    'let y = x * 3 + 4;\n';
let toks = OakLexer.tokenize(src);
let r = OakParser.parse(toks);

if r.error_count == 0 {
  print(r.root);
}

r.dispose();
