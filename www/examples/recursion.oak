fn fact(n : number) -> number {
  if n <= 1 {
    return 1;
  }
  return n * fact(n - 1);
}

fn power(base : number, exp : number) -> number {
  if exp == 0 {
    return 1;
  }
  return base * power(base, exp - 1);
}

for i from 0 to 8 {
  print(fact(i));
}

print(power(2, 10));
