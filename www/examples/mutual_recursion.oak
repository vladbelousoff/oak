fn is_even(n : number) -> number {
  if n == 0 {
    return 1;
  }
  return is_odd(n - 1);
}

fn is_odd(n : number) -> number {
  if n == 0 {
    return 0;
  }
  return is_even(n - 1);
}

for i from 0 to 8 {
  print(is_even(i));
}
